import * as controller from '../controllers'

module.exports = (app) => {

//REST API ROUTES
// retrieve         app.get('/*', controller.homeRoute )
// create / add         .post('/')                       
// update all           .put('/')                          
// update one           .patch('/')                       
// delete               .delete('/')                     

}